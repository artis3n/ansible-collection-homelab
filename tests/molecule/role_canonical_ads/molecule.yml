---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: ghcr.io/artis3n/docker-ubuntu2204-ansible:latest
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw

    cgroupns_mode: host
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
scenario:
  name: role_canonical_ads
  test_sequence:
#    - dependency
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
#    - verify
#    - cleanup
    - destroy
